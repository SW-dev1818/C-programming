배열 선언할 때 중괄호는 개수 의미

선언과 동시 초기화는 중괄호 사용

P.90,75

진도: 228~257(9.1.1까지)

char형 배열은 마지막 문자 다음에 반드시 널문자 대입('\0';)

저장한 문자열의 길이보다 최소한 하나 이상 크게 배열 선언

char형 배열은 중괄호 없이 문자열 상수로 직접 초기화 가능.

strcpy 함수가 안될 시에는 프로젝트 속성에서 SDL 검사 끄기.

system("pause")는 return 0; 위에 선언하기.

문자열 대입은 strcpy 함수 사용.

scanf 함수는 하나의 단어만 입력.

gets 함수는 한 줄 입력.

printf 함수는 문자열 출력.

puts 함수는 문자열 출력 후 줄 바꿈.

문자열 끝에 널문자가 없으면 쓰레기값을 문자열로 출력.

포인터는 변수의 메모리 주소를 저장하는 변수.

주소는 변수가 할당된 메모리 공간의 시작 주소를 사용.

포인터는 변수 앞에 *만 붙여주면 선언이 됨.