진도: 254~274(예제 9-7까지 함)

포인터 값 sizeof //4byte

%x, %p //16진수로 출력

sizeof P.118

테스트를 많이 해봐야 한다.

포인터를 선언하는 방법은 변수 앞에 *만 붙여주면 된다.

포인터 반드시 알고 넘어가자.

정수형은 4바이트

실수형은 8바이트

포인터 const를 사용하면 가리키는 변수의 값을 바꿀 수 없다.

예제 9-4:
const가 선언 됬을 시에는 *pa= 20;은 안된다

포인터는 가리키는 자료형이 일치할 때만 대입한다.

포인터는 변수이므로 그 값을 다른 주소로 바꿀 수 있다.

char //1바이트

포인터에 간접참조 연산자를 사용하여 가리키는 변수의 크기를 출력하면 각각 다른 결과가 나옵니다.

swap 함수